export default function Loading() {
  return (
    <div
      style={{
        padding: "80px 24px",
        textAlign: "center",
        fontFamily:
          "ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace",
        color: "#555",
      }}
    >
      Verifying batch…
    </div>
  );
}
